import { Filters } from "../enums";

/**
 * Configuration options for Filters.
 *
 * @property {Filters} [filterId] - The filter id for Filters application (model: Filters).
 */
export declare type FiltersConfig = {
  filterId?: Filters;
};
