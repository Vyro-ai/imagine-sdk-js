import { Filters } from "../enums";
/**
 * Configuration options for AI Filters.
 *
 * @property {Filters} [filterId] - The filter id for AI Filters application (model: Filters).
 */
export declare type AIFitlersConfig = {
    filterId?: Filters;
};
