/**
 * Input image can be a Blob or a string.
 * If a string is provided, it is assumed to be a URL, or a image local path.
 * If a Blob is provided, it is assumed to be a image file in the form of a Blob.
 *
 * @type {Blob | string} ImageParam
 */
export declare type ImageParam = Blob | string;
